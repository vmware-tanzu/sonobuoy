sonobuoy-config:
  driver: Job
  plugin-name: badName
  result-format: raw
spec:
  command:
  - ./run.sh
  image: foo
  name: plugin
  resources: {}
  volumeMounts:
  - mountPath: /tmp/results
    name: results

