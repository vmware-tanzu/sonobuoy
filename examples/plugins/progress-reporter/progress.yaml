sonobuoy-config:
  driver: Job
  plugin-name: progress
spec:
  command:
  - ./run.sh
  image: schnake/progress-reporter:v0.1
  name: plugin
  resources: {}
  volumeMounts:
  - mountPath: /tmp/results
    name: results

